#/bin/bash

for d in content/github/*/ 
do
  d=${d%/}
  [ -e build/es6-bundled/$d ] && unlink build/es6-bundled/$d;
  [ -e build/es6-unbundled/$d ] && unlink build/es6-unbundled/$d;
  ln -s $PWD/$d build/es6-bundled/$d;
  ln -s $PWD/$d build/es6-unbundled/$d;
done
